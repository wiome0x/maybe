<nav class="flex items-center gap-2">
  <%= render DS::Link.new(
    text: t("markets.stocks.section_nav.news"),
    href: market_stocks_news_path,
    variant: current_page?(market_stocks_news_path) ? "primary" : "ghost"
  ) %>
  <%= render DS::Link.new(
    text: t("markets.stocks.section_nav.heatmap"),
    href: market_stocks_heatmap_path,
    variant: current_page?(market_stocks_heatmap_path) ? "primary" : "ghost"
  ) %>
  <%= render DS::Link.new(
    text: t("markets.stocks.section_nav.watchlist"),
    href: market_stocks_path,
    variant: current_page?(market_stocks_path) ? "primary" : "ghost"
  ) %>
</nav>
